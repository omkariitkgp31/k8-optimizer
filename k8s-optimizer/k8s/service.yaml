apiVersion: v1
kind: Service
metadata:
  name: k8s-optimizer-svc
  labels:
    app: k8s-optimizer
spec:
  selector:
    app: k8s-optimizer
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
